always push changes to a new branch

git add .
git commit -m "<message>"
git checkout -b branch-name    // git branch -d branch-name {to delete}
git push origin branch-name    // git push origin --delete branch-name
